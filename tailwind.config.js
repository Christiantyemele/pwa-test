/** @type {import('tailwindcss').config} */

export default {
    purge: [],
    darkMode: false, // or 'media' or 'class'
    daisyui: {
        theme: [
            "light", "valentine", "aqua"
        ],
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
}
